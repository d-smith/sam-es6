{
  "name": "sam-es6",
  "version": "1.0.0",
  "description": "",
  "main": "src/mylambda.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && babel ./src --out-dir dist/ --presets node8 --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./readme.md,./package-lock.json --copy-files"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-node8": "^1.2.0"
  }
}
